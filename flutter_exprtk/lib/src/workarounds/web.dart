// On web, there are currently no workarounds that need to be applied
void apply() {
  // There still has to be an apply function, so that the
  // conditional import in flutter_exprtk_init.dart works!
}
